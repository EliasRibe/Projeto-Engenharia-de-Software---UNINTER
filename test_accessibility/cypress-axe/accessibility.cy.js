// Starter de acessibilidade: como não há UI HTML aqui, deixamos um placeholder
// Quando tiver uma página (ex.: http://localhost:5173), ativar o axe:
// import 'cypress-axe';
// cy.injectAxe();
// cy.checkA11y(null, { includedImpacts: ['critical','serious'] });


describe('A11y placeholder', () => {
it('placeholder para futuras verificações de acessibilidade', () => {
expect(true).to.eq(true);
});
});